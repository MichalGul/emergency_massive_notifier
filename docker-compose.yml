version: '3.7'

services:
  emn-web:
    image: gulczyn/emn_image:v1
    container_name: EMN
    command: uvicorn src.main:app --host 0.0.0.0 --port 80 --reload
    ports:
      - "8001:80"
    volumes:
      - .:/code